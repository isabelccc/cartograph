/**
 * Background job: enrich `ProductIntake.normalizedFeatures` (skeleton).
 *
 * @see ../../../../docs/COST-PREDICTION.md
 */
export async function processCostIntakeEnrichment(_intakeId: string): Promise<void> {
  // Enqueue from worker when queue is wired.
}
