/**
 * Admin HTTP routes: product intake + cost prediction (BFF) — not wired in main yet.
 *
 * @see ../../../../docs/COST-PREDICTION.md
 */
export function registerCostIntakeRoutesStub(): void {
  // Call from admin-bff `main` when BFF is composed with cost-estimation services.
}
