/**
 * tax — tax.service (service)
 *
 * Requirements:
 * - Line allocation
 * - R-DOM-1: Services use ports, not Drizzle.
 * - R-DOM-3 where applicable: state machines centralized.
 *
 * TODO:
 * - [ ] compute tax lines
 *
 * @see ../../../../docs/SERIES-B-PLATFORM.md — Domain modules — tax
 */
export function createTaxService(): never {
  throw new Error("TODO: createTaxService — see file header JSDoc");
}

