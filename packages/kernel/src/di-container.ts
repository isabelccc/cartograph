/**
 * Minimal registry for kernel-scoped services (skeleton).
 *
 * @see ../../../../docs/SERIES-B-PLATFORM.md — Kernel
 */
export function createKernelContainer(): Map<string, unknown> {
  return new Map();
}
