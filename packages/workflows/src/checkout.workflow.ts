/**
 * Checkout saga.
 *
 * Requirements:
 * - Compensation: release inventory, void payment intent.
 *
 * TODO:
 * - [ ] Steps: validate → reserve → payment session → order
 *
 * @see ../../../../docs/SERIES-B-PLATFORM.md — Workflows
 */
export function runCheckoutWorkflow(): never {
  throw new Error("TODO: runCheckoutWorkflow — see file header JSDoc");
}

