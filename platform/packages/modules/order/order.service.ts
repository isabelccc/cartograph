/**
 * order — order.service (service)
 *
 * Requirements:
 * - R-DOM-1: Services use ports, not Drizzle.
 * - R-DOM-3 where applicable: state machines centralized.
 *
 * TODO:
 * - [ ] create, cancel vs refund semantics
 *
 * @see ../../../../docs/SERIES-B-PLATFORM.md — Domain modules — order
 */
export function createOrderService(): never {
  throw new Error("TODO: createOrderService — see file header JSDoc");
}

