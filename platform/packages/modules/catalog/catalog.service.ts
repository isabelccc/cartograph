/**
 * catalog — catalog.service (service)
 *
 * Requirements:
 * - R-DOM-1
 * - R-DOM-1: Services use ports, not Drizzle.
 * - R-DOM-3 where applicable: state machines centralized.
 *
 * TODO:
 * - [ ] List, get, publish
 *
 * @see ../../../../docs/SERIES-B-PLATFORM.md — Domain modules — catalog
 */
export function createCatalogService(): never {
  throw new Error("TODO: createCatalogService — see file header JSDoc");
}

