/**
 * customer — customer.service (service)
 *
 * Requirements:
 * - R-DOM-1: Services use ports, not Drizzle.
 * - R-DOM-3 where applicable: state machines centralized.
 *
 * TODO:
 * - [ ] CRUD, groups
 *
 * @see ../../../../docs/SERIES-B-PLATFORM.md — Domain modules — customer
 */
export function createCustomerService(): never {
  throw new Error("TODO: createCustomerService — see file header JSDoc");
}

