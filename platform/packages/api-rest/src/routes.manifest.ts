/**
 * REST route manifest merge point.
 *
 * Requirements:
 * - Versioned under /v1; problem+json for errors.
 *
 * TODO:
 * - [ ] Collect route plugins from kernel.
 *
 * @see ../../../../docs/SERIES-B-PLATFORM.md — api-rest
 */
export function buildRoutesManifest(): never {
  throw new Error("TODO: buildRoutesManifest — see file header JSDoc");
}

