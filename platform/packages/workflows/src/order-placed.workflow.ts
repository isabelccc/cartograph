/**
 * Post-order placement.
 *
 * Requirements:
 * - Emit events after commit (outbox).
 *
 * TODO:
 * - [ ] Inventory commit, emails, search
 *
 * @see ../../../../docs/SERIES-B-PLATFORM.md — Workflows
 */
export function runOrderPlacedWorkflow(): never {
  throw new Error("TODO: runOrderPlacedWorkflow — see file header JSDoc");
}

