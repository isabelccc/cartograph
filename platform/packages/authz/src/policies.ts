/**
 * Admin vs customer scopes.
 *
 * Requirements:
 * - Deny by default for admin mutations
 *
 * TODO:
 * - [ ] CASL rules or custom matrix
 *
 * @see ../../../../docs/SERIES-B-PLATFORM.md — authz
 */
export function loadPolicies(): never {
  throw new Error("TODO: loadPolicies — see file header JSDoc");
}

